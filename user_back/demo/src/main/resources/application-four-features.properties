# Compatibility profile. Shared settings now live in application.properties.
# PAYMENT_CAPTURE_ENABLED / PUSH_ENABLED remain false unless explicitly enabled.
# Apply both db/manual SQL files before enabling the Android integrations.
