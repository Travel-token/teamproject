<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.back.mapper.FeedLikeMapper">

    <select id="exists" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM feed_like
        WHERE feed_post_id = #{feedPostId} AND user_id = #{userId}
    </select>

    <insert id="insert">
        INSERT INTO feed_like (feed_post_id, user_id, created_at)
        VALUES (#{feedPostId}, #{userId}, NOW())
    </insert>

    <delete id="delete">
        DELETE FROM feed_like
        WHERE feed_post_id = #{feedPostId} AND user_id = #{userId}
    </delete>

</mapper>
