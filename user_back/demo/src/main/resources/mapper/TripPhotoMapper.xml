<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.back.mapper.TripPhotoMapper">

    <!-- 여행 사진 목록 -->
    <select id="findByTripId"
            resultType="com.example.back.vo.TripPhotoVo">
        SELECT
            id,
            trip_id AS tripId,
            image_url AS imageUrl,
            created_at AS createdAt
        FROM trip_photos
        WHERE trip_id = #{tripId}
        ORDER BY created_at DESC, id DESC
    </select>

    <!-- 여행 사진 저장 -->
    <insert id="insert"
            parameterType="com.example.back.vo.TripPhotoVo"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO trip_photos (
            trip_id,
            image_url
        ) VALUES (
            #{tripId},
            #{imageUrl}
        )
    </insert>

</mapper>