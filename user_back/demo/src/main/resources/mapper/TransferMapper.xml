<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.project.mapper.TransferMapper">

    <!-- 송금 추가 -->
    <insert id="insertTransfer" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO transfers (
            trip_id, from_name, to_name, amount, spent_at, memo
        ) VALUES (
            #{tripId}, #{fromName}, #{toName}, #{amount}, #{spentAt}, #{memo}
        )
    </insert>

    <!-- 송금 목록 조회 -->
    <select id="selectTransferList" resultType="com.example.project.dto.transfer.TransferResponse$Item">
        SELECT
            id,
            from_name AS fromName,
            to_name   AS toName,
            DATE_FORMAT(spent_at, '%m월 %d일 %H:%i') AS dateLabel,
            '계좌이체' AS method,
            amount,
            memo
        FROM transfers
        WHERE trip_id = #{tripId}
        ORDER BY spent_at DESC
    </select>

    <!-- 존재 여부 -->
    <select id="existsById" resultType="int">
        SELECT COUNT(1) FROM transfers WHERE id = #{id}
    </select>

    <!-- 송금 삭제 -->
    <delete id="deleteTransfer">
        DELETE FROM transfers WHERE id = #{id}
    </delete>

</mapper>