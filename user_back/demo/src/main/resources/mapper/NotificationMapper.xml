<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.back.mapper.NotificationMapper">

    <!-- 알림 목록 -->
    <resultMap id="notificationResultMap"
               type="com.example.back.vo.notification.NotificationVo">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="tripId" column="trip_id"/>
        <result property="type" column="type"
                typeHandler="com.example.back.vo.enums.NotificationType$Handler"/>
        <result property="title" column="title"/>
        <result property="body" column="body"/>
        <result property="actionType" column="action_type"/>
        <result property="read" column="is_read"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findByUserId" resultMap="notificationResultMap">
        SELECT id, user_id, trip_id, type, title, body,
               action_type, is_read, created_at
        FROM notifications
        WHERE user_id = #{userId}
        ORDER BY created_at DESC, id DESC
    </select>

    <!-- 알림 읽음 -->
    <update id="markAsRead">
        UPDATE notifications
        SET is_read = 1
        WHERE id = #{notificationId}
          AND user_id = #{userId}
    </update>

</mapper>