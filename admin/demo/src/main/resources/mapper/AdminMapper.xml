<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.project.mapper.AdminMapper">

    <select id="findByUsername" resultType="com.example.project.domain.Admin">
        SELECT id, username, password, name, email, status, last_login_at AS lastLoginAt,
               created_at AS createdAt, updated_at AS updatedAt
        FROM admins
        WHERE username = #{username}
    </select>

    <update id="updateLastLoginAt">
        UPDATE admins
        SET last_login_at = NOW()
        WHERE id = #{id}
    </update>

</mapper>
