// Compatibility helpers. Login must save the full pair using saveSession, not only an access token.
export { ensureAccessToken as getToken, forgetSession as removeToken, saveSession } from './authSession';